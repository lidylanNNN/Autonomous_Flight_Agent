# M0 — Freeze Scope + Eval Spec

Status: DONE

## Scope

Defined by `DEV_SPEC.md`.

## Deliverables

- `MissionEvalCase` schema.
- 20-30 development mission cases under `mission_sets/`.
- `EnvironmentManifest` contract.
- Dataset split / freeze rules.
- Schema validation script.
- M0 review notes.

## Interfaces Changed

- `mission_sets/schemas/mission_case.py`: `MissionEvalCase` and mission set manifest schema.
- `mission_sets/schemas/split_manifest.py`: family-level split manifest schema.
- `manifests/schemas/environment_manifest.py`: typed `EnvironmentManifest` contract.

## Tests

- `python3 -m compileall -q scripts manifests mission_sets`
- `UV_PYTHON_INSTALL_DIR=.uv-python uv --cache-dir .uv-cache run --python /usr/bin/python3 python scripts/validation/validate_environment_manifest.py`
- `UV_PYTHON_INSTALL_DIR=.uv-python uv --cache-dir .uv-cache run --python /usr/bin/python3 python scripts/validation/validate_mission_sets.py`
- `UV_PYTHON_INSTALL_DIR=.uv-python uv --cache-dir .uv-cache run --python /usr/bin/python3 python scripts/validation/validate_mission_splits.py`

## Mock Status

No runtime mock is implemented in M0.

M0 only defines contracts and fixture-shaped mission data. `MockFlightRuntime`,
`MockLLMProvider`, and `MockHumanApproval` are planned in later milestones.

## Evaluation

M0 evaluation is contract-level only:

- Environment manifest validates.
- Mission case schema validates.
- Mission split manifests validate.
- Family-level leakage guard validates.

No PX4 / ROS2 / Gazebo episode has run yet. Task Success, Safety, and Recovery
metrics are still design targets, not achieved results.

## Known Limitations

- Current repository is a M0 skeleton.
- PX4 / ROS2 / Gazebo runtime is not implemented yet.
- Metrics are not achieved results until backed by real evaluation reports.

## Artifacts

- `manifests/environment.yaml`
- `manifests/schemas/environment_manifest.py`
- `mission_sets/dev/manifest.json`
- `mission_sets/validation/manifest.json`
- `mission_sets/frozen_test/manifest.json`
- `docs/mission_set_docs/split_freeze_rules.md`
- `mission_sets/dev/DEV-T*.json`
- `mission_sets/schemas/mission_case.py`
- `mission_sets/schemas/split_manifest.py`
- `scripts/validation/validate_environment_manifest.py`
- `scripts/validation/validate_mission_sets.py`
- `scripts/validation/validate_mission_splits.py`

## Git Commit

- `e0a81cd data: add mission split freeze rules`
- Freeze commit: this M0 review commit.

## DoD

- [x] V1 scope and non-goals are defined in `DEV_SPEC.md`.
- [x] Safety, verifier, recovery, trace, and evaluation contracts are defined.
- [x] First dev mission set contains 30 JSON cases.
- [x] `MissionEvalCase` schema exists.
- [x] `EnvironmentManifest` schema exists.
- [x] Family-level split / freeze rules exist.
- [x] Validation scripts pass for environment, mission cases, and split manifests.
- [x] Known limitation is explicit: runtime and real metrics are not implemented in M0.

## Open Bad Cases

- None for M0 contract/data validation.
- Runtime bad cases will be discovered after M1-M3 produce executable flight paths.

## Next

Start M1: PX4 + ROS2 + Gazebo reproducible runtime.
