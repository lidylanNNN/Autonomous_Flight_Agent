# M0 — 冻结范围与 Evaluation Spec

状态：DONE

## 范围

以 `DEV_SPEC.md` 为准。

## 交付物

- `MissionEvalCase` Schema。
- `agent_benchmark_sets/` 下的 20–30 条 Agent 开发测评任务。
- `EnvironmentManifest` Contract。
- Dataset Split / Freeze Rules。
- Schema Validation Script。
- M0 Review Notes。

## 接口变更

- `agent_benchmark_sets/schemas/mission_case.py`：`MissionEvalCase` 和测评集 Manifest Schema。
- `agent_benchmark_sets/schemas/split_manifest.py`：Family-level Split Manifest Schema。
- `manifests/schemas/environment_manifest.py`：强类型 `EnvironmentManifest` Contract。

## 测试

- `python3 -m compileall -q scripts manifests agent_benchmark_sets`
- `UV_PYTHON_INSTALL_DIR=.uv-python uv --cache-dir .uv-cache run --python /usr/bin/python3 python scripts/validation/validate_environment_manifest.py`
- `UV_PYTHON_INSTALL_DIR=.uv-python uv --cache-dir .uv-cache run --python /usr/bin/python3 python scripts/validation/validate_agent_benchmark_sets.py`
- `UV_PYTHON_INSTALL_DIR=.uv-python uv --cache-dir .uv-cache run --python /usr/bin/python3 python scripts/validation/validate_agent_benchmark_splits.py`

## Mock 状态

M0 不实现 Runtime Mock。

M0 只定义 Contract 和 Fixture 形态的 Mission Data。`MockFlightExecutionRuntime`、
`MockLLMProvider` 和 `MockHumanApproval` 分别在后续里程碑交付。

## Evaluation

M0 只进行 Contract-level Evaluation：

- Environment Manifest 校验通过。
- Mission Case Schema 校验通过。
- Mission Split Manifest 校验通过。
- Family-level 数据泄漏防护校验通过。

此时尚未运行 PX4/ROS2/Gazebo Episode。Task Success、Safety 和 Recovery 指标仍是
设计目标，不是已取得的结果。

## 已知限制

- 当前仓库只是 M0 骨架。
- 尚未实现 PX4/ROS2/Gazebo Runtime。
- 所有指标必须由真实 Evaluation Report 支撑后才能作为成果。

## 产物

- `manifests/environment.yaml`
- `manifests/schemas/environment_manifest.py`
- `agent_benchmark_sets/dev/manifest.json`
- `agent_benchmark_sets/validation/manifest.json`
- `agent_benchmark_sets/frozen_test/manifest.json`
- `docs/agent_benchmark_docs/split_freeze_rules.md`
- `agent_benchmark_sets/dev/DEV-T*.json`
- `agent_benchmark_sets/schemas/mission_case.py`
- `agent_benchmark_sets/schemas/split_manifest.py`
- `scripts/validation/validate_environment_manifest.py`
- `scripts/validation/validate_agent_benchmark_sets.py`
- `scripts/validation/validate_agent_benchmark_splits.py`

## Git Commit

- `e0a81cd data: add mission split freeze rules`
- Freeze Commit：M0 Review Commit。

## DoD

- [x] `DEV_SPEC.md` 已定义 V1 Scope 和 Non-goals。
- [x] 已定义 Safety、Verifier、Recovery、Trace 和 Evaluation Contract。
- [x] 首批 Dev Mission Set 包含 30 条 JSON Case。
- [x] `MissionEvalCase` Schema 已建立。
- [x] `EnvironmentManifest` Schema 已建立。
- [x] Family-level Split / Freeze Rules 已建立。
- [x] Environment、Mission Case 和 Split Manifest Validation Script 通过。
- [x] 已明确 Runtime 与真实指标尚未在 M0 实现。

## Open Bad Case

- M0 Contract/Data Validation 没有未解决 Bad Case。
- Runtime Bad Case 等待 M1–M3 产生可执行飞行链路后发现和记录。

## 下一里程碑

M1 — PX4 + ROS2 + Gazebo 可复现 Runtime。
